# Browser uses same-origin proxy (see next.config.ts rewrites)
NEXT_PUBLIC_API_URL=/api/v1
# Where Next proxies /api/v1 → Express
API_PROXY_TARGET=http://localhost:4001

NEXT_PUBLIC_GOOGLE_CLIENT_ID=
NEXT_PUBLIC_SENTRY_DSN=

# Server-only (Vercel)
SENTRY_AUTH_TOKEN=
